import _mapValues from 'lodash/mapValues';


export default function sanitizeMapEvent(e) {
  return _mapValues(e, function (val, key) {
    switch (key) {
      case 'originalEvent':
      case 'target':
        return '[' + key + ']';
      default:
        return val;
    }
  });
}